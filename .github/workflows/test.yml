name: test

on: [push, pull_request]

jobs:
    simple:
        name: Install and test simple.nix
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v2
            - name: Install and cache Nix packages 
              uses: ./
              with:
                nix_file: 'test/simple.nix'
            - name: Test whether simple.nix is installed
              run: hello
